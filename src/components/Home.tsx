import Navigation from "../Navigation";

const Home = () => {
    return (
        <div>
            <h2>React Beta版公式ドキュメントを読んでいく。</h2>
            <Navigation />
        </div>
    );
};

export default Home;