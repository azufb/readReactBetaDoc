import { Link } from "react-router-dom"

const Navigation = () => {
    return (
        <ul>
            <li>
                <Link to='stateHooks'>State Hooks</Link>
            </li>
        </ul>
    );
};

export default Navigation;